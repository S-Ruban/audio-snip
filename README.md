# audio-snip
Extracts individual songs from albums on Youtube (songs are marked as "chapters" in the video).

Uses [```pydub```](https://pypi.org/project/pydub/) to slice the original album file in mp3, then add metadata to individual songs
